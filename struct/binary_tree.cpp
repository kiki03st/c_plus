#include <iostream>

using namespace std;

struct bin{
	int data;
	int l_r;
	bin  *left, *right;
};

void head(bin *p){
	p -> l_r = 0;
}

bin* append(bin *tail, int data){
	if(tail -> l_r == 0){	//head
		tail -> data = 
int main(){
	bin *p = new(bin);
	bin *tail = p;
	tail = append(10)
	return 0;
}
