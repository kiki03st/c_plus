#include <iostream>

using namespace std;

int main(){
	int var[4];
	// 배열 속 요소들의 공간 할당은 뒤에서부터 진행됨
	// Ex) var[3], var[2], var[1], var[0] 순서대로 스택에 할당됨
	int var3;
	int var2;
	int var1;
	int var0;
	// 변수의 선언은 선언되는 순서대로 스택에 쌓임
	// Ex) var3, var2, var1, var0 순서대로 스택에 할당됨
	return 0;
}
