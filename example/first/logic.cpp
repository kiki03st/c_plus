#include <iostream>

using namespace std;

int main(){
	int a = 0, b = 1;

	cout << "a and b : " << (a and b) << endl;
	cout << "a or b : " << (a or b) << endl;
	cout << "not a : " << (not a) << endl;
	//C++에서는 파이썬과 동일하게 and, or, not 키워드 사용 가능
	return 0;
}
